build:
  main: ./cmd/phoneserver/
  binary: phoneserver
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

archive:
  files:
    - LICENSE
    - README.md

dockers:
  -
    dockerfile: cmd/phoneserver/Dockerfile
    image: nyaruka/phoneserver
    binary: phoneserver
    tag_templates:
      - "{{ .Tag }}"
      - latest